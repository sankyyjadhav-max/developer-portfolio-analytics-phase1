"use client";import Link from 'next/link';import {usePathname,useRouter} from 'next/navigation';import {LayoutDashboard,UserRound,FolderKanban,Palette,Settings,LogOut,Menu,X,Sun,Moon} from 'lucide-react';import {useState} from 'react';import {useTheme} from 'next-themes';import {api} from '../lib/api';import {cn} from '../lib/cn';import {Button} from './ui';
const nav=[['/dashboard','Overview',LayoutDashboard],['/dashboard/portfolio','Portfolio',UserRound],['/dashboard/projects','Projects',FolderKanban],['/dashboard/appearance','Appearance',Palette],['/dashboard/settings','Settings',Settings]] as const;
export function Shell({children,userName,title}:{children:React.ReactNode;userName:string;title:string}){const path=usePathname();const router=useRouter();const [open,setOpen]=useState(false);const {theme,setTheme}=useTheme();const logout=async()=>{await api('/auth/logout',{method:'POST'});router.push('/login')};return <div className="min-h-screen"><aside className={cn('fixed inset-y-0 left-0 z-40 w-64 border-r border-[var(--line)] bg-[var(--panel)] p-5 transition-transform lg:translate-x-0',open?'translate-x-0':'-translate-x-full')}><div className="mb-8 flex items-center justify-between"><Link href="/dashboard" className="font-semibold tracking-tight">Portfolio Studio</Link><button className="lg:hidden" onClick={()=>setOpen(false)}><X size={18}/></button></div><nav className="grid gap-1">{nav.map(([href,label,Icon])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={cn('flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm transition',path===href?'bg-neutral-100 font-medium dark:bg-neutral-900':'text-neutral-500 hover:bg-neutral-50 hover:text-neutral-900 dark:hover:bg-neutral-900 dark:hover:text-white')}><Icon size={17}/>{label}</Link>)}</nav><div className="absolute bottom-5 left-5 right-5 grid gap-1"><button onClick={()=>setTheme(theme==='dark'?'light':'dark')} className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-neutral-500 hover:bg-neutral-50 dark:hover:bg-neutral-900">{theme==='dark'?<Sun size={17}/>:<Moon size={17}/>} {theme==='dark'?'Light mode':'Dark mode'}</button><button onClick={logout} className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-neutral-500 hover:bg-neutral-50 dark:hover:bg-neutral-900"><LogOut size={17}/> Log out</button></div></aside><main className="lg:pl-64"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-[var(--line)] bg-[var(--bg)]/95 px-5 backdrop-blur lg:px-8"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={()=>setOpen(true)}><Menu size={20}/></button><div><p className="text-sm font-semibold">{title}</p></div></div><div className="flex items-center gap-3"><span className="hidden text-sm text-neutral-500 sm:block">{userName}</span><div className="grid h-8 w-8 place-items-center rounded-full bg-neutral-900 text-xs font-semibold text-white dark:bg-white dark:text-neutral-900">{userName?.[0]?.toUpperCase()}</div></div></header><div className="mx-auto max-w-7xl px-5 py-8 lg:px-8">{children}</div></main></div>}