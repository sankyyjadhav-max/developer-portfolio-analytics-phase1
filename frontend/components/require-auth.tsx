"use client";import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {api} from '../lib/api';import {User} from '../types';export function RequireAuth({children}:{children:React.ReactNode}){const [user,setUser]=useState<User|null>(null);const [loading,setLoading]=useState(true);const router=useRouter();useEffect(()=>{api('/auth/me').then(r=>setUser(r.data.user)).catch(()=>router.replace('/login')).finally(()=>setLoading(false))},[router]);if(loading)return <div className="grid min-h-screen place-items-center text-sm text-neutral-500">Loading your workspace…</div>;return user?<>{children}</>:null}