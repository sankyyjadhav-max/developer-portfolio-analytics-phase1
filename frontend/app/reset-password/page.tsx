"use client";import Link from 'next/link';import {useSearchParams,useRouter} from 'next/navigation';import {useState} from 'react';import {Input,Button} from '../../components/ui';import {api} from '../../lib/api';export default function Reset(){const q=useSearchParams();const router=useRouter();const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [err,setErr]=useState('');const [busy,setBusy]=useState(false);async function submit(e:any){e.preventDefault();if(password!==confirm)return setErr('Passwords do not match');setBusy(true);try{await api('/auth/reset-password',{method:'POST',body:JSON.stringify({token:q.get('token'),password})});router.push('/login')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center px-5"><div className="w-full max-w-sm"><Link href="/login" className="text-sm">← Back to sign in</Link><h1 className="mt-10 text-3xl font-semibold">Choose a new password</h1><form onSubmit={submit} className="mt-8 grid gap-5"><Input label="New password" type="password" minLength={8} required value={password} onChange={(e:any)=>setPassword(e.target.value)}/><Input label="Confirm password" type="password" minLength={8} required value={confirm} onChange={(e:any)=>setConfirm(e.target.value)}/>{err&&<p className="text-sm text-red-600">{err}</p>}<Button disabled={busy}>{busy?'Updating…':'Update password'}</Button></form></div></main>}