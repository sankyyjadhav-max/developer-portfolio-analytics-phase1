import {Router} from 'express';import * as c from '../controllers/projects';import {auth} from '../middleware/auth';const r=Router();r.get('/',auth,c.list);r.post('/',auth,c.create);r.get('/:id',auth,c.get);r.put('/:id',auth,c.update);r.delete('/:id',auth,c.remove);export default r;